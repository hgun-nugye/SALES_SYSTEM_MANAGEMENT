@model QuanLyBanHang.Models.NhaCCDetailView

@{
    ViewBag.Title = "Xóa Nhà Cung Cấp";
}


<div class=" container">
    <h4 class="text-danger mb-3">
        Xóa Nhà Cung Cấp
    </h4>

    <div class="alert alert-warning shadow-sm">
        <i class="fa-solid fa-triangle-exclamation"></i>
        Bạn có chắc muốn xóa Nhà cung cấp <strong>@Model.TenNCC</strong> không?
    </div>

    <div class="border p-4 rounded shadow-sm bg-light">
        <p><strong>@Html.DisplayNameFor(m => m.MaNCC):</strong> @Model.MaNCC</p>
        <p><strong>@Html.DisplayNameFor(m => m.TenNCC):</strong> @Model.TenNCC</p>
        <p><strong>@Html.DisplayNameFor(m => m.DienThoaiNCC):</strong> @Model.DienThoaiNCC</p>
        <p><strong>@Html.DisplayNameFor(m => m.EmailNCC):</strong> @Model.EmailNCC</p>
        <p><strong>@Html.DisplayNameFor(m => m.DiaChiNCC):</strong> @Model.DiaChiNCC, @Model.TenXa, @Model.TenTinh</p>

    </div>

    <form id="deleteForm" asp-action="Delete" method="post" class="mt-4">
        <input type="hidden" asp-for="MaNCC" />
        <button type="button" class="btn btn-danger" onclick="confirmDelete()">
            <i class="fa-solid fa-trash-can me-2"></i> Xóa
        </button>
        <a asp-action="Details" asp-route-id="@Model.MaNCC" class="btn btn-secondary">
            <i class="fa-solid fa-arrow-left"></i> Quay lại
        </a>
    </form>

</div>
@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>
        function confirmDelete() {
            Swal.fire({
                title: 'Xác nhận xóa?',
                text: 'Bạn có chắc chắn muốn xóa nhà cung cấp này?',
                icon: 'warning',
                showCancelButton: true,
                confirmButtonText: 'Xóa',
                cancelButtonText: 'Hủy',
                confirmButtonColor: '#d33',
                cancelButtonColor: '#3085d6'
            }).then((result) => {
                if (result.isConfirmed) {
                    document.getElementById('deleteForm').submit();
                }
            });
        }
    </script>
}
