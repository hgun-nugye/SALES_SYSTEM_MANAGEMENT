@model IEnumerable<QuanLyBanHang.Models.NhomSP>

@{
    ViewBag.Title = "Quản lý nhóm sản phẩm";
}

<div class="container">
    <!-- Header -->
    <div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center mb-4 gap-3">
        <div>
            
        </div>
        
        <div class="d-flex gap-2">
            <a href="@Url.Action("Index", "LoaiSP")" class="btn btn-outline-success shadow-sm">
                <i class="fa-solid fa-folder-tree me-2"></i>Chi tiết Loại SP
            </a>
            <a href="@Url.Action("Create", "NhomSP")" class="btn btn-success shadow-sm">
                <i class="fa-solid fa-plus-circle me-2"></i>Thêm nhóm mới
            </a>
        </div>
    </div>

    <!-- Filter Card -->
    <div class="md-card mb-4 bg-light border-0">
        <form id="filterForm" method="get" class="row g-3 align-items-end">
            <div class="col-md-6">
                <label class="form-label small fw-bold text-muted text-uppercase">Tìm nhóm sản phẩm</label>
                <div class="input-group bg-white border rounded-3 overflow-hidden">
                    <span class="input-group-text bg-transparent border-0"><i class="fa-solid fa-magnifying-glass opacity-50"></i></span>
                    <input id="searchBox" type="text" name="search" value="@ViewBag.Search" class="form-control border-0" placeholder="Ví dụ: Điện tử, Thời trang, Gia dụng...">
                </div>
            </div>
            <div class="col-md-auto ms-auto">
                <button type="button" id="btnReset" class="btn btn-light border px-4">
                    <i class="fa-solid fa-rotate-left me-2"></i>Làm mới
                </button>
            </div>
        </form>
    </div>

    <!-- Data Table -->
    <div class="table-responsive shadow-sm">
        <table class="table table-hover mb-0">
            <thead>
                <tr>
                    <th class="ps-4" style="width: 20%">Mã Nhóm</th>
                    <th>Tên Nhóm Sản Phẩm</th>
                </tr>
            </thead>
            <tbody>
                @if (Model != null && Model.Any())
                {
                    @foreach (var item in Model)
                    {
                        <tr style="cursor: pointer" onclick="window.location='@Url.Action("Details", "NhomSP", new { id = item.MaNhom })'">
                            <td class="ps-4">
                                <span class="badged bg-light text-dark font-monospace border">#@item.MaNhom</span>
                            </td>
                            <td>
                                <div class="fw-bold text-dark fs-6">@item.TenNhom</div>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="3" class="py-5 text-center text-muted">
                            <i class="fa-solid fa-layer-group fs-1 d-block mb-3 opacity-25"></i>
                            Chưa có dữ liệu nhóm sản phẩm nào được tạo.
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@section Scripts {
    <script>
        const filterForm = document.getElementById('filterForm');
        document.getElementById('searchBox').addEventListener('input', debounce(() => filterForm.submit(), 500));
        document.getElementById('btnReset').addEventListener('click', () => {
            window.location.href = '@Url.Action("Index", "NhomSP")';
        });

        function debounce(fn, delay) {
            let timer = null;
            return function() {
                const context = this, args = arguments;
                clearTimeout(timer);
                timer = setTimeout(() => fn.apply(context, args), delay);
            };
        }
    </script>
}
